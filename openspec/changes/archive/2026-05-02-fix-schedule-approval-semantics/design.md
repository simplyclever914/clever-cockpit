# Design

Approval gates are opt-in for schedules. A schedule may still create ideas while running. If a discovered idea needs human decision, it can create a separate idea review approval without blocking the scheduled job itself.
