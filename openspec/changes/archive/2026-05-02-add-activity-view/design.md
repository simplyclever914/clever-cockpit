# Design

Activity reuses existing `state.activity` and detail rendering. Inbox can remain a mixed attention view, while Activity is the canonical event log for runs, approvals, idea transitions, and outcomes.
